git add filename   把文件添加到仓库
git commit -m "statement"   把文件提交到仓库 
git status    查看未提交的修改
git diff filename   查看修改   
git init  创建版本库
git reflog  查看命令历史
git log --pretty=oneline   显示最近到最远的提交日志
git reset --hard HEAD^(commit id)  版本回退
git checkout --filename   撤销修改
git remote add origin https://github.com/nanhua097/learning.git 与远程库建立连接
git @github.com:nanhua97/learing.git   与远程库建立连接
git clone git@github.com:nanhua97/learning.git  先哦嗯远程克隆分支
git push -u origin master  远程推送分支
git checkout dev/master  切换分支
git checkout -b dev 创建分支并切换
git branch dev    创建分支
git branch -d dev   删除分支
git merge <name>    合并某分支到当前分支   
git branch -D dev   删除未合并的分支
git merge --no-ff -m "merge with no-ff" dev  
git log --graph --pretty=oneline --abbrev-commit   查看分支历史
git pull 从远程库得到更新
git push 先远程库推送更新
git stash 保存当前工作进程
git stash list
git stash pop  恢复保留的工作进程并删除保存
git branch --set-upstream-to dev origin/dev 建立本地分支与远程分支的关联
git checkout -b dev origin/dev 创建和远程分支对应的本地分支
git tag <name> 新建一个标签 
git tag 标签列表
git show <tagname> 显示标签信息
git tag -a <tagname> -m "statement" 指定标签信息
git push origin <tagname> 推送某个标签到远程库
git push origin --tags 推送所有标签到远程库
git tag -d <tagname> 删除本地标签
git push origin :res/tags/<tagname> 将删除标签操作推送到远程库























